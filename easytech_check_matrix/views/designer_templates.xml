<odoo>
    <template id="check_template_designer_page" name="EasyTech Check Template Designer">
        <html>
            <head>
                <meta charset="utf-8"/>
                <title><t t-esc="labels['page_title']"/></title>
                <style>
                    body { font-family: Arial, sans-serif; margin: 0; background: #f5f7fb; }
                    .wrap { max-width: 1350px; margin: 18px auto; padding: 0 16px; }
                    .topbar { display:flex; justify-content:space-between; align-items:center; margin-bottom:12px; }
                    .title { font-size: 20px; font-weight: 700; }
                    .layout { display:flex; gap:16px; align-items:flex-start; }
                    .panel { width: 340px; background:#fff; border:1px solid #d9e0ea; border-radius:8px; padding:10px; }
                    .panel h4 { margin: 6px 0 10px; }
                    .row { display:grid; grid-template-columns: 1fr 1fr; gap:8px; margin-bottom:8px; }
                    .row label { font-size:12px; color:#334; display:block; margin-bottom:2px; }
                    .row input { width:100%; padding:6px; border:1px solid #c8d0dc; border-radius:4px; }
                    .canvas-wrap { flex:1; background:#fff; border:1px solid #d9e0ea; border-radius:8px; padding:12px; overflow:auto; }
                    .check-canvas { position:relative; background:#fff; border:1px solid #999; transform-origin: top left; }
                    .field { position:absolute; background:rgba(38,132,255,.10); border:1px dashed #2684ff; border-radius:4px; padding:2px 6px; cursor:move; user-select:none; font-size:12px; z-index:3; }
                    .preview-item { position:absolute; color:#111; font-size:13px; z-index:2; pointer-events:none; white-space:nowrap; }
                    .preview-item.amount-text { max-width:300px; white-space:normal; line-height:1.2; }
                    .preview-item.date { letter-spacing:2px; }
                    .preview-item.beneficiary { width:272px; white-space:nowrap; overflow:hidden; }
                    .preview-item.amount-num { width:132px; white-space:nowrap; overflow:hidden; }
                    .actions { margin-top:10px; display:flex; gap:8px; }
                    .btn { border:0; border-radius:6px; padding:8px 12px; cursor:pointer; }
                    .btn-primary { background:#1677ff; color:#fff; }
                    .btn-light { background:#eef2f8; color:#223; }
                    .hint { font-size:12px; color:#667; margin-top:8px; }
                    .ok { color:#128a2e; font-weight:700; }
                </style>
            </head>
            <body>
                <div id="et_i18n" style="display:none"
                     t-att-data-saved="labels['msg_saved']"
                     t-att-data-err="labels['msg_error_prefix']"
                     t-att-data-unknown="labels['msg_unknown']"/>
                <div class="wrap">
                    <div class="topbar">
                        <div class="title"><t t-esc="labels['page_title']"/></div>
                        <div class="actions">
                            <a class="btn btn-light" t-att-href="'/report/pdf/easytech_check_matrix.report_easytech_check_preview_template/%s' % t.id" target="_blank"><t t-esc="labels['preview']"/></a>
                            <a class="btn btn-light" t-att-href="'/report/pdf/easytech_check_matrix.report_easytech_check_calibration/%s' % t.id" target="_blank"><t t-esc="labels['calibration']"/></a>
                            <a class="btn btn-light" t-att-href="'/web#id=%s&amp;model=easytech.check.template&amp;view_type=form' % t.id"><t t-esc="labels['back_template']"/></a>
                        </div>
                    </div>
                    <div class="layout">
                        <div class="panel">
                            <h4><t t-esc="labels['positions_mm']"/></h4>
                            <div class="row"><div><label><t t-esc="labels['check_no_x']"/></label><input id="check_no_left_mm" t-att-value="t.check_no_left_mm"/></div><div><label><t t-esc="labels['check_no_y']"/></label><input id="check_no_top_mm" t-att-value="t.check_no_top_mm"/></div></div>
                            <div class="row"><div><label><t t-esc="labels['date_x']"/></label><input id="date_left_mm" t-att-value="t.date_left_mm"/></div><div><label><t t-esc="labels['date_y']"/></label><input id="date_top_mm" t-att-value="t.date_top_mm"/></div></div>
                            <div class="row"><div><label><t t-esc="labels['payee_x']"/></label><input id="beneficiary_left_mm" t-att-value="t.beneficiary_left_mm"/></div><div><label><t t-esc="labels['payee_y']"/></label><input id="beneficiary_top_mm" t-att-value="t.beneficiary_top_mm"/></div></div>
                            <div class="row"><div><label><t t-esc="labels['amount_num_x']"/></label><input id="amount_num_left_mm" t-att-value="t.amount_num_left_mm"/></div><div><label><t t-esc="labels['amount_num_y']"/></label><input id="amount_num_top_mm" t-att-value="t.amount_num_top_mm"/></div></div>
                            <div class="row"><div><label><t t-esc="labels['amount_words_x']"/></label><input id="amount_txt_left_mm" t-att-value="t.amount_txt_left_mm"/></div><div><label><t t-esc="labels['amount_words_y']"/></label><input id="amount_txt_top_mm" t-att-value="t.amount_txt_top_mm"/></div></div>
                            <div class="row"><div><label><t t-esc="labels['offset_x']"/></label><input id="offset_left_mm" t-att-value="t.offset_left_mm"/></div><div><label><t t-esc="labels['offset_y']"/></label><input id="offset_top_mm" t-att-value="t.offset_top_mm"/></div></div>
                            <div class="actions">
                                <button class="btn btn-primary" id="saveBtn"><t t-esc="labels['save']"/></button>
                                <button class="btn btn-light" id="refreshBtn"><t t-esc="labels['refresh']"/></button>
                            </div>
                            <div class="hint"><t t-esc="labels['hint']"/> <span id="status"></span></div>
                        </div>
                        <div class="canvas-wrap">
                            <div class="check-canvas" id="canvas" t-att-style="'width:%spx;height:%spx;' % (t.page_width_mm * scale, t.page_height_mm * scale)">
                                <div class="field" data-x="check_no_left_mm" data-y="check_no_top_mm"><t t-esc="labels['overlay_check']"/></div>
                                <div class="field" data-x="date_left_mm" data-y="date_top_mm"><t t-esc="labels['overlay_date']"/></div>
                                <div class="field" data-x="beneficiary_left_mm" data-y="beneficiary_top_mm"><t t-esc="labels['overlay_payee']"/></div>
                                <div class="field" data-x="amount_num_left_mm" data-y="amount_num_top_mm"><t t-esc="labels['overlay_amount_num']"/></div>
                                <div class="field" data-x="amount_txt_left_mm" data-y="amount_txt_top_mm"><t t-esc="labels['overlay_amount_words']"/></div>

                                <div class="preview-item" data-x="check_no_left_mm" data-y="check_no_top_mm"><strong>000123</strong></div>
                                <div class="preview-item date" data-x="date_left_mm" data-y="date_top_mm">26032026</div>
                                <div class="preview-item beneficiary" data-x="beneficiary_left_mm" data-y="beneficiary_top_mm">SAMPLE COMPANY S A</div>
                                <div class="preview-item amount-num" data-x="amount_num_left_mm" data-y="amount_num_top_mm">963.00 PAB</div>
                                <div class="preview-item amount-text" data-x="amount_txt_left_mm" data-y="amount_txt_top_mm"><t t-esc="t.env._('Nine hundred sixty-three balboa')"/></div>
                            </div>
                        </div>
                    </div>
                </div>
                <script>
                    const templateId = <t t-esc="t.id"/>;
                    const scale = <t t-esc="scale"/>;
                    const fields = Array.from(document.querySelectorAll('.field'));
                    const statusEl = document.getElementById('status');
                    const i18nEl = document.getElementById('et_i18n');
                    const i18n = i18nEl ? i18nEl.dataset : {};

                    function toNum(v){ const n = parseFloat(v); return isNaN(n) ? 0 : n; }
                    function getVal(id){ return toNum(document.getElementById(id).value); }
                    function setVal(id,v){ document.getElementById(id).value = (Math.round(v*100)/100).toString(); }

                    function applyPositions(){
                        const ox = getVal('offset_left_mm');
                        const oy = getVal('offset_top_mm');
                        fields.forEach(el => {
                            const x = getVal(el.dataset.x) + ox;
                            const y = getVal(el.dataset.y) + oy;
                            el.style.left = `${x * scale}px`;
                            el.style.top = `${y * scale}px`;
                        });
                        document.querySelectorAll('.preview-item').forEach(el => {
                            const x = getVal(el.dataset.x) + ox;
                            const y = getVal(el.dataset.y) + oy;
                            el.style.left = `${x * scale}px`;
                            el.style.top = `${y * scale}px`;
                        });
                    }

                    function setupDrag(el){
                        let startX=0,startY=0,origX=0,origY=0,drag=false;
                        el.addEventListener('pointerdown', (e)=>{
                            drag=true; el.setPointerCapture(e.pointerId);
                            startX=e.clientX; startY=e.clientY;
                            origX=getVal(el.dataset.x); origY=getVal(el.dataset.y);
                        });
                        el.addEventListener('pointermove', (e)=>{
                            if(!drag) return;
                            const dx=(e.clientX-startX)/scale;
                            const dy=(e.clientY-startY)/scale;
                            setVal(el.dataset.x, origX+dx);
                            setVal(el.dataset.y, origY+dy);
                            applyPositions();
                        });
                        el.addEventListener('pointerup', ()=>{ drag=false; });
                    }

                    fields.forEach(setupDrag);
                    document.getElementById('refreshBtn').addEventListener('click', applyPositions);
                    document.querySelectorAll('input').forEach(i => i.addEventListener('input', applyPositions));

                    document.getElementById('saveBtn').addEventListener('click', async ()=>{
                        const values = {};
                        [
                            'offset_top_mm','offset_left_mm','check_no_top_mm','check_no_left_mm','date_top_mm','date_left_mm',
                            'beneficiary_top_mm','beneficiary_left_mm','amount_num_top_mm','amount_num_left_mm',
                            'amount_txt_top_mm','amount_txt_left_mm'
                        ].forEach(k => values[k] = getVal(k));
                        const r = await fetch('/easytech/check-template/save', {
                            method: 'POST',
                            headers: {'Content-Type':'application/json'},
                            body: JSON.stringify({jsonrpc:'2.0', method:'call', params:{template_id: templateId, values}, id: Date.now()})
                        });
                        const j = await r.json();
                        const res = j.result || j;
                        const unk = i18n.unknown || 'unknown';
                        statusEl.textContent = res.ok ? (i18n.saved || '') : ((i18n.err || 'Error:') + ' ' + (res.error || unk));
                        statusEl.className = res.ok ? 'ok' : '';
                    });

                    applyPositions();
                </script>
            </body>
        </html>
    </template>
</odoo>
